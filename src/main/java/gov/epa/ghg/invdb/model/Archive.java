package gov.epa.ghg.invdb.model;

import jakarta.persistence.Entity;
import jakarta.persistence.Id;

@Entity
public class Archive {
        @Id
        private Long id; // A dummy ID that is never used

}
